<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ProyectoF.Views"
       x:Class="ProyectoF.AppShell">

    <ShellContent Title="Tipo de Usuario"
                  ContentTemplate="{DataTemplate views:ElegirUsuarioPage}"
                  Route="ElegirUsuarioPage" />

    <ShellContent Title="LogIn"
                  ContentTemplate="{DataTemplate views:LogInPage}"
                  Route="LogInPage" />

    <ShellContent Title="Inicio"
                  ContentTemplate="{DataTemplate views:InicioMaestroPage}"
                  Route="InicioMaestroPage" />

    <ShellContent Title="Inicio"
                  ContentTemplate="{DataTemplate views:InicioEstudiantePage}"
                  Route="InicioEstudiantePage" />

    <ShellContent Title="Inicio"
                  ContentTemplate="{DataTemplate views:InicioAdminPage}"
                  Route="InicioAdminPage" />

</Shell>